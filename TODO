Rework plugins/http to return Promises
in 'make site' have less pre-compile CSS, instead of using less.js on the page

add $.Progress(N).timeout(ms)
	after ms, the Promise is rejected with a special timeout error
	? future calls to .resolve() could throw an error (so workers can stop working)
